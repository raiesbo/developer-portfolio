import AboutData from "./AboutData";

type Profile = {
    name: string,
    surname: string,
    nickname: string,
    email: string,
    website: string,
    archive: string,
    main_description: string,
    about: AboutData
};

export default Profile;
